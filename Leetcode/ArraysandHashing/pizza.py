print("""==========================================================
            Welcome to FeatherDB - A Lightweight SQL Database Engine
==========================================================

FeatherDB is a simple, file-based SQL database engine.
It supports basic SQL operations like:

  - CREATE TABLE
  - INSERT INTO
  - SELECT
  - UPDATE
  - DELETE

You can interact with FeatherDB using the following commands:

- CREATE TABLE table_name (column1 datatype, column2 datatype, ...);
- INSERT INTO table_name VALUES (value1, value2, ...);
- SELECT * FROM table_name;
- UPDATE table_name SET column1 = value WHERE condition;
- DELETE FROM table_name WHERE condition;

To get started, type your SQL command at the prompt.

For help or to quit, type:

  - HELP    : Show this welcome message again
  - EXIT    : Exit the FeatherDB program

==========================================================
FeatherDB> 
==========================================================""")